# 1. 포트 설정 (1883은 MQTT 기본 포트)
listener 1883 0.0.0.0

# 2. 익명 접속 허용 (개발 단계에서 필수)
# 이 옵션이 없으면 접속할 때마다 아이디/비밀번호를 입력해야 합니다.
allow_anonymous true

# 3. 데이터 저장 (브로커가 재시작되어도 메시지 상태 유지)
persistence true
persistence_location /mosquitto/data/

# 4. 로그 설정 (에러나 연결 상태를 확인하기 위함)
log_dest stdout
log_type all